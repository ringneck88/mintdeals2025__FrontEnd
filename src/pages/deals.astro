---
export const prerender = true;

import Layout from '../layouts/Layout.astro';

// API Configuration
const DISCOUNTS_API_URL = 'https://zealous-adaptation-production-ee61.up.railway.app/api/products/discounts';
const API_BASE = 'https://mintdealsbackend-production.up.railway.app/api';

// Initialize data
let discountProducts = [];
let stores = [];
let storeInfo = null;
let apiError = null;

// Helper function to get discount badge info
function getDiscountBadge(discount) {
  if (!discount) return { text: 'Special', color: 'from-yellow-500 to-orange-500' };

  if (discount.discountType === 'Percent') {
    return {
      text: `${discount.discountAmount}% OFF`,
      color: 'from-green-500 to-emerald-500'
    };
  } else if (discount.discountType === 'Price To Amount - Total Amount' || discount.discountType === 'Price To Amount') {
    return {
      text: `$${discount.discountAmount.toFixed(2)}`,
      color: 'from-yellow-500 to-orange-500'
    };
  }

  return { text: 'Special Deal', color: 'from-purple-500 to-pink-500' };
}

// Helper function to format date
function formatDate(dateString) {
  if (!dateString) return '';
  const date = new Date(dateString);
  return date.toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' });
}

// Fetch stores with location data
try {
  console.log('🚀 Fetching stores from API');

  const storesResponse = await fetch(`${API_BASE}/stores?populate=*&pagination[pageSize]=100`);

  if (storesResponse.ok) {
    const data = await storesResponse.json();
    const storesData = data?.data || [];

    stores = storesData.map(store => {
      // Extract coordinates from geo field
      let latitude = null;
      let longitude = null;

      if (store.geo) {
        if (typeof store.geo === 'object') {
          latitude = store.geo.lat || store.geo.latitude || null;
          longitude = store.geo.lng || store.geo.lon || store.geo.longitude || null;
        } else if (typeof store.geo === 'string') {
          try {
            const geoObj = JSON.parse(store.geo);
            latitude = geoObj.lat || geoObj.latitude || null;
            longitude = geoObj.lng || geoObj.lon || geoObj.longitude || null;
          } catch (e) {
            console.error('Failed to parse geo string for store:', store.name);
          }
        }
      }

      return {
        id: store.id,
        documentId: store.documentId,
        name: store.name || 'Unnamed Store',
        slug: store.slug || store.documentId,
        address: store.address || {},
        phone: store.phone,
        latitude: latitude,
        longitude: longitude,
        isActive: store.is_active !== false
      };
    }).filter(store => store.isActive && store.latitude && store.longitude);

    console.log(`✅ Loaded ${stores.length} stores with coordinates`);
  }
} catch (error) {
  console.error('❌ Error fetching stores:', error);
}

// Fetch discount products
let productsByStore = {};
let totalProductsCount = 0;

try {
  console.log('🚀 Fetching discount products from API');

  const response = await fetch(DISCOUNTS_API_URL);

  if (response.ok) {
    const data = await response.json();

    discountProducts = data.data || [];

    // Group products by their storeId from the API response
    discountProducts.forEach((product) => {
      const storeId = product.storeId ? String(product.storeId) : 'unknown';
      const storeName = product.storeName || 'Unknown Store';

      // Find matching store from stores array by ID or name
      let matchingStore = null;
      for (const s of stores) {
        if (String(s.id) === storeId || s.name === storeName) {
          matchingStore = s;
          break;
        }
      }

      // Create store group if it doesn't exist
      if (!productsByStore[storeId]) {
        productsByStore[storeId] = {
          storeId: storeId,
          storeName: storeName,
          store: matchingStore || { id: storeId, name: storeName, address: {} },
          products: []
        };
      }

      // Add product to this store's products array
      productsByStore[storeId].products.push(product);
    });

    // Calculate total products (6 per store max)
    totalProductsCount = Object.values(productsByStore).reduce((sum, storeGroup) => {
      return sum + Math.min(storeGroup.products.length, 6);
    }, 0);

    const storeCount = Object.keys(productsByStore).length;
    console.log(`✅ Loaded ${discountProducts.length} products from ${storeCount} stores`);
    Object.values(productsByStore).forEach((store) => {
      console.log('   📍 ' + store.storeName + ': ' + store.products.length + ' products');
    });
  } else {
    const errorData = await response.text();
    console.error('❌ Discounts API Error:', errorData);
    apiError = 'Failed to fetch discount products';
  }
} catch (error) {
  console.error('❌ Error fetching discounts:', error);
  apiError = error.message;
}
---

<style>
  body {
    margin: 0;
    padding: 0;
  }
  .video-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    overflow: hidden;
  }
  .video-background::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: linear-gradient(to top, var(--theme-bg-start), var(--theme-bg-end));
    mix-blend-mode: color;
    pointer-events: none;
    z-index: 1;
    transition: background-image 0.5s ease;
  }
  .video-background video {
    object-fit: cover;
    height: 100%;
    width: 100%;
  }
  .content-wrapper {
    position: relative;
    z-index: 1;
    background: linear-gradient(to bottom, rgba(0,0,0,0.2), rgba(0,0,0,0.4));
  }
  .hero-section {
    min-height: 7.5vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0.5rem 0;
  }

  .hero-section h1 {
    line-height: 1.2;
  }

  @media (min-width: 768px) {
    .hero-section {
      min-height: 30vh;
      padding: 1rem 0;
    }
  }

  .line-clamp-2 {
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .line-clamp-3 {
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  /* Product card hover effects */
  .product-card {
    transition: all 0.3s ease;
  }

  .product-card:hover {
    transform: translateY(-8px);
  }

  .product-card img {
    transition: transform 0.5s ease;
  }

  .product-card:hover img {
    transform: scale(1.1);
  }
</style>

<Layout title="Deals & Specials - Cannabis Dispensary Promotions | Mint Deals" description="Discover the best cannabis deals and specials. Save on premium cannabis products at Mint Cannabis dispensaries.">
  <!-- Video Background -->
  <div class="video-background">
    <video autoplay loop muted playsinline>
      <source src="https://video.wixstatic.com/video/d47472_58cce06729c54ccb935886c4b3647274/1080p/mp4/file.mp4" type="video/mp4">
    </video>
  </div>

  <!-- Content Wrapper -->
  <div class="content-wrapper">
    <!-- Hero Section -->
    <div class="text-white hero-section">
      <div class="px-8 mx-auto sm:px-12 lg:px-16 xl:px-20 max-w-[90%]">
        <!-- Breadcrumb -->
        <nav class="mb-6 text-sm">
          <div class="flex items-center justify-center space-x-2">
            <a href="/" class="text-yellow-400 hover:text-yellow-300 transition-colors">Home</a>
            <span class="text-green-300">/</span>
            <span class="text-white">Deals</span>
          </div>
        </nav>

        <div class="text-center">
          <h1 class="mb-4 text-4xl font-bold md:text-6xl drop-shadow-2xl">
            <span class="inline-block pb-2 leading-tight bg-gradient-to-r from-yellow-400 to-orange-500 bg-clip-text text-transparent uppercase">
              DEALS & SPECIALS
            </span>
          </h1>
          <p class="max-w-2xl mx-auto mb-6 text-lg text-yellow-400 md:text-xl drop-shadow-lg">
            Discover the best cannabis deals and save on premium products
          </p>
        </div>
      </div>
    </div>

    <!-- Store Info Bar -->
    {Object.keys(productsByStore).length > 0 && (
      <div class="py-6 bg-black/90">
        <div class="px-8 mx-auto sm:px-12 lg:px-16 xl:px-20 max-w-[90%]">
          <div class="grid grid-cols-1 gap-6 text-center md:grid-cols-3">
            <div class="p-4 border border-green-500 rounded-lg bg-green-900/20">
              <div id="total-products-count" class="mb-2 text-3xl font-bold text-green-400">{totalProductsCount}</div>
              <div class="text-sm text-yellow-400 font-semibold uppercase">Products on Sale</div>
            </div>
            <div class="p-4 border border-green-500 rounded-lg bg-green-900/20">
              <div class="mb-2 text-xl font-bold text-green-400">{Object.keys(productsByStore).length}</div>
              <div class="text-sm text-yellow-400 font-semibold uppercase">{Object.keys(productsByStore).length === 1 ? 'Store with Deals' : 'Stores with Deals'}</div>
            </div>
            <div class="p-4 border border-green-500 rounded-lg bg-green-900/20">
              <div class="mb-2 text-xl font-bold text-green-400">💰</div>
              <div class="text-sm text-yellow-400 font-semibold uppercase">Save Big Today</div>
            </div>
          </div>
        </div>
      </div>
    )}

    <!-- Main Content -->
    <div class="py-16">
      <div class="px-8 mx-auto sm:px-12 lg:px-16 xl:px-20 max-w-[90%]">

        {apiError ? (
          <div class="py-12 text-center">
            <div class="max-w-md p-8 mx-auto bg-red-900 rounded-lg bg-opacity-80">
              <div class="mb-4 text-6xl text-red-400">⚠️</div>
              <h3 class="mb-2 text-lg font-medium text-red-200">API Error</h3>
              <p class="text-sm text-red-300">{apiError}</p>
              <div class="mt-6">
                <a
                  href="/stores"
                  class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-yellow-400 to-transparent text-white rounded-lg font-semibold hover:from-yellow-500 hover:to-transparent transition-all transform hover:scale-105 shadow-xl"
                >
                  Browse Locations
                </a>
              </div>
            </div>
          </div>
        ) : Object.keys(productsByStore).length === 0 ? (
          <div class="text-center py-16">
            <div class="max-w-md p-8 mx-auto bg-black border-2 border-green-500 rounded-lg">
              <div class="mb-4 text-6xl">💸</div>
              <h3 class="mt-2 text-lg font-medium text-yellow-400">
                No deals available right now
              </h3>
              <p class="mt-2 text-sm text-green-300">
                Check back soon for amazing deals and specials!
              </p>
              <div class="mt-6">
                <a
                  href="/stores"
                  class="inline-flex items-center px-6 py-3 bg-gradient-to-r from-yellow-400 to-transparent text-white rounded-lg font-semibold hover:from-yellow-500 hover:to-transparent transition-all transform hover:scale-105 shadow-xl"
                >
                  Browse Locations
                </a>
              </div>
            </div>
          </div>
        ) : (
          <div id="stores-container">
            {/* Loop through each store and display its products */}
            {Object.values(productsByStore).map((storeGroup) => (
              <div class="mb-16 store-section" data-store-id={storeGroup.storeId}>
                {/* Store Header */}
                <div class="mb-6 p-6 bg-gradient-to-r from-green-900/50 to-black border border-green-500 rounded-lg">
                  <div class="flex items-center justify-between flex-wrap gap-4">
                    <div>
                      <h2 class="text-2xl md:text-3xl font-bold text-white mb-2">
                        <span class="inline-block pb-1 leading-tight bg-gradient-to-r from-yellow-400 to-orange-500 bg-clip-text text-transparent uppercase">
                          {storeGroup.storeName}
                        </span>
                      </h2>
                      {storeGroup.store?.address && (
                        <p class="text-green-300 flex items-center gap-2">
                          <svg class="w-4 h-4 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
                          </svg>
                          {(storeGroup.store.address.city && storeGroup.store.address.state)
                            ? `${storeGroup.store.address.city}, ${storeGroup.store.address.state}`
                            : 'Location Available'}
                        </p>
                      )}
                    </div>
                    <div class="flex items-center gap-3">
                      <span class="px-4 py-2 bg-yellow-400/20 border border-yellow-400 rounded-full text-sm font-bold text-yellow-400">
                        {Math.min(storeGroup.products.length, 6)} Deals
                      </span>
                      {storeGroup.store?.slug && (
                        <a
                          href={`/location/${storeGroup.store.slug}`}
                          class="inline-flex items-center px-4 py-2 bg-gradient-to-r from-yellow-400 to-transparent text-white rounded-lg font-semibold hover:from-yellow-500 hover:to-transparent transition-all shadow-xl text-sm"
                        >
                          Store Info
                          <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                          </svg>
                        </a>
                      )}
                    </div>
                  </div>
                </div>

                {/* Products Grid for this store */}
                <div class="products-grid grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 xl:grid-cols-6 gap-4">
                  {storeGroup.products.slice(0, 6).map((product, index) => {
                    // Cycle through available discounts so each card shows a different one
                    const discountIndex = product.discounts?.length > 0 ? index % product.discounts.length : 0;
                    const primaryDiscount = product.discounts?.[discountIndex];
                    const badge = getDiscountBadge(primaryDiscount);

                    return (
                      <div class="product-card relative overflow-hidden bg-black border border-green-500 rounded-lg shadow-lg hover:shadow-2xl hover:border-yellow-400 group" data-store-id={storeGroup.storeId}>
                    {/* Red Banner with Discount Name */}
                    {primaryDiscount && (
                      <div class="w-full bg-gradient-to-r from-red-600 to-red-700 py-1.5 px-2">
                        <div class="text-white font-bold text-[10px] text-center uppercase truncate">
                          {primaryDiscount.discountName}
                        </div>
                      </div>
                    )}

                    {/* Product Image */}
                    <div class="relative h-40 bg-gradient-to-br from-gray-900 to-black overflow-hidden">
                      {product.productImageUrl ? (
                        <img
                          src={product.productImageUrl}
                          alt={product.productName}
                          class="w-full h-full object-cover"
                          loading="lazy"
                        />
                      ) : (
                        <div class="flex items-center justify-center w-full h-full">
                          <span class="text-6xl opacity-30">🌿</span>
                        </div>
                      )}
                      {/* Overlay gradient */}
                      <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-transparent"></div>
                    </div>

                    {/* Product Info */}
                    <div class="p-2">
                      {/* Brand */}
                      {product.productBrand && (
                        <div class="mb-1">
                          <span class="text-[9px] font-semibold text-green-400 uppercase">
                            {product.productBrand}
                          </span>
                        </div>
                      )}

                      {/* Product Name */}
                      <h3 class="text-white font-bold text-xs mb-1 line-clamp-2 min-h-[2rem]">
                        {product.productName}
                      </h3>

                      {/* Category */}
                      {product.productCategory && (
                        <div class="mb-2">
                          <span class="inline-block px-1.5 py-0.5 text-[9px] font-medium text-yellow-400 bg-yellow-400/10 border border-yellow-400/30 rounded">
                            {product.productCategory}
                          </span>
                        </div>
                      )}

                      {/* Price */}
                      <div class="flex items-center justify-center mb-2">
                        <div>
                          <span class="text-lg font-bold text-green-400">
                            ${product.productPrice.toFixed(2)}
                          </span>
                        </div>
                      </div>

                      {/* Discount Details */}
                      {primaryDiscount && primaryDiscount.validUntil && (
                        <div class="mb-2 p-1.5 bg-green-900/20 border border-green-500/30 rounded">
                          <div class="text-[9px] text-yellow-400 text-center">
                            ⏰ Valid until {formatDate(primaryDiscount.validUntil)}
                          </div>
                        </div>
                      )}

                      {/* Shop Now Button */}
                      <a
                        href="/stores"
                        class="block w-full text-center px-2 py-2 bg-gradient-to-r from-yellow-400 to-transparent text-white rounded text-xs font-semibold hover:from-yellow-500 hover:to-transparent transition-all transform hover:scale-105 shadow-xl"
                      >
                        Shop Now
                        <svg class="inline w-3 h-3 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path>
                        </svg>
                      </a>
                    </div>
                  </div>
                    );
                  })}
                </div>
              </div>
            ))}

            <!-- Call to Action -->
            <div class="mt-16 bg-black border border-yellow-400 rounded-lg p-8 text-center">
              <h2 class="text-2xl font-bold mb-4">
                <span class="inline-block pb-1 leading-tight bg-gradient-to-r from-yellow-400 to-orange-500 bg-clip-text text-transparent uppercase">
                  Don't Miss Out on These Amazing Deals!
                </span>
              </h2>
              <p class="text-green-300 mb-6 max-w-2xl mx-auto">
                Visit your nearest Mint Cannabis location to take advantage of these special offers. Limited time only!
              </p>
              <div class="flex flex-wrap justify-center gap-4">
                <a
                  href="/stores"
                  class="bg-gradient-to-r from-yellow-400 to-transparent text-white px-8 py-3 rounded-lg font-semibold hover:from-yellow-500 hover:to-transparent transition-all transform hover:scale-105 shadow-xl"
                >
                  Find a Location
                </a>
                <a
                  href="/categories"
                  class="bg-black border border-green-500 text-green-300 px-8 py-3 rounded-lg font-semibold hover:bg-green-900/20 hover:border-yellow-400 transition-colors"
                >
                  Shop by Category
                </a>
              </div>
            </div>
          </div>
        )}
      </div>
    </div>
  </div>

  <!-- Minimal script for deals page - geolocation removed for performance -->
  <script>
    console.log('📍 Deals page loaded - showing all stores');
  </script>
</Layout>
