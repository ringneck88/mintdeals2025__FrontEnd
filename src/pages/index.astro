---
import Layout from '../layouts/Layout.astro';

// Initialize data arrays
let stores = [];
let categories = [];
let apiError = null;

// Category icon mapping
function getCategoryIcon(slug) {
  const icons = {
    'flower': '🌿',
    'pre-rolls': '🚬',
    'vaporizers': '💨',
    'concentrates': '🍯',
    'edibles': '🍭',
    'topicals': '🧴',
    'cartridges': '🖊️'
  };
  return icons[slug] || '🌿';
}

const API_BASE = 'https://mintdealsbackend-production.up.railway.app/api';

try {
  console.log('🚀 Fetching stores and categories from API');

  // Fetch stores and categories in parallel
  const [storesResponse, categoriesResponse] = await Promise.allSettled([
    fetch(`${API_BASE}/locations?populate=*&pagination[pageSize]=6`),
    fetch(`${API_BASE}/product-categories?populate=*&pagination[pageSize]=8&sort=Name:asc`)
  ]);

  // Process stores
  if (storesResponse.status === 'fulfilled' && storesResponse.value.ok) {
    const data = await storesResponse.value.json();
    const storesData = data?.data || [];

    stores = storesData.map(store => ({
      id: store.id,
      documentId: store.documentId,
      name: store.Name || 'Unnamed Store',
      slug: store.Slug || store.documentId,
      address: store.Address,
      phone: store.Phone,
      hours: store.Hours,
      image: store.HeroImage?.url ? `https://mintdealsbackend-production.up.railway.app${store.HeroImage.url}` : null,
      isActive: store.is_active !== false
    })).filter(store => store.isActive);

    console.log('✅ Stores loaded:', stores.length);
  }

  // Process categories
  if (categoriesResponse.status === 'fulfilled' && categoriesResponse.value.ok) {
    const data = await categoriesResponse.value.json();
    const categoriesData = data?.data || [];

    categories = categoriesData.map(category => ({
      id: category.id,
      documentId: category.documentId,
      title: category.Name || '',
      description: category.Description || category.Discription || '',
      slug: category.Slug || category.documentId,
      icon: getCategoryIcon(category.Slug),
      image: category.Image?.url ? `https://mintdealsbackend-production.up.railway.app${category.Image.url}` : null,
      isActive: category.IsActive !== false
    })).filter(category => category.title && category.isActive);

    console.log('✅ Categories loaded:', categories.length);
  }

} catch (error) {
  console.error('❌ Error fetching data:', error);
  apiError = error.message;
}
---
---

<style>
  body {
    margin: 0;
    padding: 0;
  }
  .video-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    z-index: -1;
    overflow: hidden;
  }
  .video-background::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: linear-gradient(to top, #ffffff, #c2f0c2, #70db70, #33cc33, #c2f0c2, #ffffff);
    mix-blend-mode: color;
    pointer-events: none;
    z-index: 1;
  }
  .video-background video {
    object-fit: cover;
    height: 100%;
    width: 100%;
  }
  .content-wrapper {
    position: relative;
    z-index: 1;
    background: linear-gradient(to bottom, rgba(0,0,0,0.3), rgba(0,0,0,0.5));
  }
  .hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }
</style>

<Layout title="Mint Deals - Cannabis Dispensaries & Products" description="Find cannabis dispensaries, products, and deals. Your trusted source for premium cannabis products and information.">
  <!-- Video Background -->
  <div class="video-background">
    <video autoplay loop muted playsinline>
      <source src="https://video.wixstatic.com/video/d47472_58cce06729c54ccb935886c4b3647274/1080p/mp4/file.mp4" type="video/mp4">
    </video>
  </div>

  <!-- Content Wrapper -->
  <div class="content-wrapper">
    <!-- Hero Section -->
    <div class="text-white hero-section">
    <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
      <div class="text-center">
        <h1 class="mb-6 text-4xl font-bold md:text-6xl">
          Mint Cannabis Deals
        </h1>
        <p class="max-w-3xl mx-auto mb-8 text-xl text-green-100 md:text-2xl">
          Your trusted source for premium cannabis products and information
        </p>
        <div class="flex flex-wrap justify-center gap-4">
          <a href="/locations" class="px-8 py-3 text-lg font-semibold text-green-800 transition-colors bg-white rounded-lg hover:bg-gray-100">
            Find Locations
          </a>
          <a href="/categories" class="px-8 py-3 text-lg font-semibold text-white transition-colors border-2 border-white rounded-lg hover:bg-white hover:text-green-800">
            Browse Products
          </a>
        </div>
      </div>
    </div>
    </div>

    <!-- Quick Stats Section -->
  <div class="py-16 bg-white dark:bg-gray-800">
    <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
      <div class="grid grid-cols-2 gap-8 text-center md:grid-cols-4">
        <div>
          <div class="mb-2 text-3xl font-bold text-green-600 dark:text-green-400">{stores.length}+</div>
          <div class="text-gray-600 dark:text-gray-400">Locations</div>
        </div>
        <div>
          <div class="mb-2 text-3xl font-bold text-green-600 dark:text-green-400">{categories.length}+</div>
          <div class="text-gray-600 dark:text-gray-400">Categories</div>
        </div>
        <div>
          <div class="mb-2 text-3xl font-bold text-green-600 dark:text-green-400">100%</div>
          <div class="text-gray-600 dark:text-gray-400">Legal</div>
        </div>
        <div>
          <div class="mb-2 text-3xl font-bold text-green-600 dark:text-green-400">24/7</div>
          <div class="text-gray-600 dark:text-gray-400">Available</div>
        </div>
      </div>
    </div>
  </div>

  <!-- Featured Locations Section -->
  <div class="py-16 bg-gray-50 dark:bg-gray-900">
    <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
      <div class="mb-12 text-center">
        <h2 class="mb-4 text-3xl font-bold text-gray-900 md:text-4xl dark:text-white">
          Featured Locations
        </h2>
        <p class="text-xl text-gray-600 dark:text-gray-300">
          Visit our trusted dispensary partners
        </p>
      </div>

      {apiError ? (
        <div class="py-12 text-center">
          <div class="max-w-md p-8 mx-auto rounded-lg bg-red-50 dark:bg-red-900">
            <div class="mb-4 text-6xl text-red-600 dark:text-red-400">⚠️</div>
            <h3 class="mb-2 text-lg font-medium text-red-800 dark:text-red-200">API Error</h3>
            <p class="text-sm text-red-600 dark:text-red-300">{apiError}</p>
          </div>
        </div>
      ) : stores.length > 0 ? (
        <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
          {stores.slice(0, 6).map(store => (
            <div class="overflow-hidden transition-shadow bg-white rounded-lg shadow-lg dark:bg-gray-800 hover:shadow-xl">
              {store.image && (
                <img
                  src={store.image}
                  alt={store.name}
                  class="object-cover w-full h-48"
                />
              )}
              <div class="p-6">
                <h3 class="mb-2 text-xl font-bold text-gray-900 dark:text-white">{store.name}</h3>
                {store.address && (
                  <p class="flex items-start mb-2 text-gray-600 dark:text-gray-300">
                    <svg class="w-5 h-5 mr-2 mt-0.5 text-green-600 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                    </svg>
                    {store.address.city}, {store.address.state}
                  </p>
                )}
                {store.phone && (
                  <p class="flex items-center mb-4 text-gray-600 dark:text-gray-300">
                    <svg class="w-5 h-5 mr-2 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                    </svg>
                    {store.phone}
                  </p>
                )}
                <a href={`/location/${store.slug}`} class="inline-flex items-center font-medium text-green-600 hover:text-green-700">
                  View Details
                  <svg class="w-4 h-4 ml-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                  </svg>
                </a>
              </div>
            </div>
          ))}
        </div>
      ) : (
        <div class="py-12 text-center">
          <div class="max-w-md p-8 mx-auto bg-white rounded-lg dark:bg-gray-800">
            <h3 class="mb-2 text-lg font-medium text-gray-900 dark:text-white">No Locations Available</h3>
            <p class="text-gray-600 dark:text-gray-300">Location information will be displayed here.</p>
          </div>
        </div>
      )}

      <div class="mt-12 text-center">
        <a href="/locations" class="inline-flex items-center px-6 py-3 font-medium text-white transition-colors bg-green-600 rounded-lg hover:bg-green-700">
          View All Locations
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <!-- Product Categories Section -->
  <div class="py-16 bg-white dark:bg-gray-900">
    <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
      <div class="mb-12 text-center">
        <h2 class="mb-4 text-3xl font-bold text-gray-900 md:text-4xl dark:text-white">
          Product Categories
        </h2>
        <p class="text-xl text-gray-600 dark:text-gray-300">
          Explore our comprehensive range of cannabis products
        </p>
      </div>

      {apiError ? (
        <div class="py-12 text-center">
          <div class="max-w-md p-8 mx-auto rounded-lg bg-red-50 dark:bg-red-900">
            <div class="mb-4 text-6xl text-red-600 dark:text-red-400">⚠️</div>
            <h3 class="mb-2 text-lg font-medium text-red-800 dark:text-red-200">API Error</h3>
            <p class="text-sm text-red-600 dark:text-red-300">{apiError}</p>
          </div>
        </div>
      ) : categories.length > 0 ? (
        <div class="grid grid-cols-2 gap-6 md:grid-cols-4">
          {categories.slice(0, 8).map(category => (
            <a href={`/${category.slug}`} class="p-6 transition-all bg-white rounded-lg shadow-lg group dark:bg-gray-800 hover:shadow-xl hover:-translate-y-1">
              <div class="text-center">
                <div class="mb-4 text-4xl">{category.icon}</div>
                <h3 class="mb-2 text-lg font-semibold text-gray-900 dark:text-white group-hover:text-green-600 dark:group-hover:text-green-400">
                  {category.title}
                </h3>
                <p class="text-sm text-gray-600 dark:text-gray-400">
                  Learn more
                </p>
              </div>
            </a>
          ))}
        </div>
      ) : (
        <div class="py-12 text-center">
          <div class="max-w-md p-8 mx-auto bg-white rounded-lg dark:bg-gray-800">
            <div class="mb-4 text-6xl">🌿</div>
            <h3 class="mb-2 text-lg font-medium text-gray-900 dark:text-white">Categories Coming Soon</h3>
            <p class="text-gray-600 dark:text-gray-300">Product categories will be available here.</p>
          </div>
        </div>
      )}

      <div class="mt-12 text-center">
        <a href="/categories" class="inline-flex items-center px-6 py-3 font-medium text-white transition-colors bg-green-600 rounded-lg hover:bg-green-700">
          View All Categories
          <svg class="w-4 h-4 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
        </a>
      </div>
    </div>
  </div>

  <!-- About Section -->
  <div class="py-16 bg-gray-50 dark:bg-gray-800">
    <div class="px-4 mx-auto sm:px-6 lg:px-8 max-w-7xl">
      <div class="mb-12 text-center">
        <h2 class="mb-4 text-3xl font-bold text-gray-900 md:text-4xl dark:text-white">
          Why Choose Mint Deals
        </h2>
        <p class="text-xl text-gray-600 dark:text-gray-300">
          Your trusted partner in cannabis discovery
        </p>
      </div>

      <div class="grid grid-cols-1 gap-8 md:grid-cols-3">
        <!-- Feature 1 -->
        <div class="p-8 bg-white rounded-lg shadow-lg dark:bg-gray-900">
          <div class="flex items-center justify-center w-16 h-16 mb-6 bg-green-100 rounded-lg dark:bg-green-900">
            <svg class="w-8 h-8 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
          </div>
          <h3 class="mb-3 text-xl font-bold text-gray-900 dark:text-white">Find Locations</h3>
          <p class="text-gray-600 dark:text-gray-300">
            Discover trusted dispensaries near you with detailed information and directions.
          </p>
        </div>

        <!-- Feature 2 -->
        <div class="p-8 bg-white rounded-lg shadow-lg dark:bg-gray-900">
          <div class="flex items-center justify-center w-16 h-16 mb-6 bg-green-100 rounded-lg dark:bg-green-900">
            <svg class="w-8 h-8 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"></path>
            </svg>
          </div>
          <h3 class="mb-3 text-xl font-bold text-gray-900 dark:text-white">Browse Products</h3>
          <p class="text-gray-600 dark:text-gray-300">
            Explore comprehensive product information and educational resources.
          </p>
        </div>

        <!-- Feature 3 -->
        <div class="p-8 bg-white rounded-lg shadow-lg dark:bg-gray-900">
          <div class="flex items-center justify-center w-16 h-16 mb-6 bg-green-100 rounded-lg dark:bg-green-900">
            <svg class="w-8 h-8 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
          </div>
          <h3 class="mb-3 text-xl font-bold text-gray-900 dark:text-white">Trusted Information</h3>
          <p class="text-gray-600 dark:text-gray-300">
            Access reliable, up-to-date information from verified sources and experts.
          </p>
        </div>
      </div>
    </div>
  </div>
  </div>
</Layout>